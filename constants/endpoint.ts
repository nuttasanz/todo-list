import { API_BASE_URL, API_JSON_PLACEHOLDER } from ".";

export const ENDPOINT = {
  REGISTER: `${API_BASE_URL}/auth/register`,
  LOGIN: `${API_BASE_URL}/auth/login`,
  USER: `${API_BASE_URL}/user`,
  TODOS: `${API_JSON_PLACEHOLDER}/todos`,
};
